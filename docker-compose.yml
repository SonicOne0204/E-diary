services:
  db:
    image: postgres:17
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - ./.env
    ports:
      - "5432:5432"
  
  backend:
    build: .
    env_file:
      - ./.env
    depends_on:
      - db
    ports:
      - "5000:5000"
    
volumes:
  pgdata: